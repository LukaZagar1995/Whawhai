import React from "react";
import { View, StyleSheet, Text } from "react-native";

const AvatarSetupScreen = () => {
  return (
    <View>
      <Text>Avatar Setup Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AvatarSetupScreen;