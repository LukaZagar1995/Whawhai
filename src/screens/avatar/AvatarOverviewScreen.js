import React from "react";
import { View, StyleSheet, Text } from "react-native";

const AvatarOverviewScreen = () => {
  return (
    <View>
      <Text>Avatar Overview Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AvatarOverviewScreen;
